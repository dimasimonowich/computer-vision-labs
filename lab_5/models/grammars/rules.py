def vconcat(terms, result):
    upper = terms[0]
    lower = terms[1]

    return upper, lower, result

def hconcat(terms, result):
    right = terms[0]
    left = terms[1]

    return right, left, result
